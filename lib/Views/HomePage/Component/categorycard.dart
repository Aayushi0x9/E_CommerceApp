import 'package:flutter/material.dart';

Widget catagoryCard() {
  return Card(
    child: Column(
      children: [
        Image.network('(link unavailable)'),
        Text('Category Name'),
      ],
    ),
  );
}
